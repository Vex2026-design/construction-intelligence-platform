export default function PlaceholderPage({ title }) {
  return (
    <div className="page">
      <section className="panel placeholder">
        <h1>{title}</h1>
        <p>Modulo previsto nella roadmap successiva.</p>
      </section>
    </div>
  );
}
